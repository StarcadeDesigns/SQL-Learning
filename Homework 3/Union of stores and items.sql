select name
from Stores

-- a union would have shown me all results as 1 output instead of 2 separate ones. 
--union all

select name
from Items

-- Please provide me 2 separate sets of results that show me all store names, and that show all item names